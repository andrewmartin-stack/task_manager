asgiref==3.8.1
Django==4.2.30
# Pin to a djangorestframework version available on PyPI for this environment
# (3.17.1 was not found on some indexes). Use 3.15.2 which is compatible.
djangorestframework==3.15.2
sqlparse==0.5.5
# django-mcp-server is not available on some private PyPI mirrors. Use
# the GitHub source to ensure installs succeed in restricted environments.
git+https://github.com/omarbenhamid/django-mcp-server.git@0.5.7#egg=django-mcp-server
