Project TODO

- Add MCP tools for other models and refine Task tool filters (low priority)
- Add authentication for MCP endpoint if exposing outside local machine (medium)
- Add integration tests for MCP tool responses and error cases (high)

The initial MCP integration and basic Task tools were added so OpenClaw
can interact locally. Revisit these items when expanding MCP usage.
